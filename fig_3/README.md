analyze_data.R = R script to analyze simulated data
fig3stats.csv = results from analyze_data.R
k_means_centroids.csv = the derived group means from the k-means clustering, used to simulate data
sim_data/ = directory with the raw simulated data
simulate_data.R = function to simulate data